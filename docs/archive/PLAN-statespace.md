# 方案(彻底版):状态空间物理采样规划,消灭格子

## 决策依据

1. **稳定是寻路的根本** —— whatever cost。
2. **格子先天不匹配 Terraria**:有重力 + 连续碰撞 + 速度依赖。"能否跳到某处"取决于当前 vx,格子丢速度必错。
3. **未来玩家机动会增加**(钩爪/翅膀/二段跳/加速靴/骑乘)。格子方案每加一种机动要手写一套边+执行+半格处理,维护成本线性爆炸。状态空间方案:新机动 = 多一个输入,框架零改动。

## 核心架构:消灭翻译层

现在的 bug 根源是两次翻译:真实位置→格子(规划)→真实位置(执行),每次丢精度。
状态空间规划**没有翻译**:规划和执行在同一个真实物理世界。半砖/斜坡/亚像素/速度依赖全自动正确。

## 地基(已有,复用)

`PhysicsSimulator` 已经是真实物理引擎:
- `State { Px,Py,Vx,Vy,Grounded,JumpFramesLeft }` — 连续状态,正是所需
- `Step(state,input,params)` — 单帧推进,原生 Collision(半砖/斜坡/水自动对)
- `Params.FromPlayer` — 真实物理参数(已含 wet)
- `ControlInput { Left,Right,Jump }` — 输入

缺的只是把它从"验证单条边"升级成"驱动搜索"。

## 设计

### 1. 搜索状态
```
SearchState {
  float Px, Py, Vx, Vy;      // 连续物理状态
  bool Grounded;
  // (未来)机动相关:bool HasGrapple, int WingTimeLeft, ...
}
```
node 是**连续物理状态**,不是格子。

### 2. 扩展:枚举输入 × 模拟
从一个 SearchState,枚举"可做的动作",每个用 PhysicsSimulator 正向模拟到下一个**决策点**:
- 运动输入原语:`{走左, 走右, 站立}` × `{跳(hold 8/12/15/...), 不跳}`
- 模拟若干帧直到"决策点"(落地 / 到达速度稳态 / 撞墙),得到新 SearchState
- 每次扩展记录:这段用的输入序列(用于执行重放) + cost(帧数/资源)

**新连续机动(钩爪/翅膀/二段跳)= 加一个运动输入原语 + Params/Step 支持它。搜索自动会用。**

### 2b. 建造(挖/放平台)= 宏动作边,不是逐帧输入原语

**踩坑结论(2026-06):放置不能当逐帧输入原语。** 走/跳是连续低维输入(方向×hold),
forward-sim 一个 Step 全搞定。放置是离散高维、改世界状态的动作:位置×时机有几十×几十种,
且"放过的平台"进 node 会让状态空间从有限维变成组合爆炸。实测把放置塞进逐帧扩展导致:
状态指数爆炸(placed 进 key)、垒柱第一块放不下(placeY 永远脚下格,两头不讨好)、
能解目标 exp 打满。这是"用前向连续搜索做离散建造"的结构性不匹配,补丁填不完。

**新方案:建造退出搜索原语,变成搜索可调用的宏动作边。**
- 搜索只做走/跳(它已证明能做好的)。
- 遇到"走跳后仍离目标有垂直落差且够不到",生成一条**宏动作边**(目标导向触发):
  - **垒柱宏**:从当前站立点,套路化逐级垒平台到目标高度。第一块放脚边(紧贴当前地面,
    CanPlace 必成立),跳上→再放。套路写死,不靠逐帧碰运气 → 第一块放不下的 bug 从根消失。
  - **搭桥宏**:水平逐格放平台跨缺口。
- 宏动作 = **1 条边 + 参数(高度/距离)**,搜索里 1 个分支,不爆炸。
- 宏边自带放置序列(套路生成时即确定哪帧放哪格),执行层照放,时机由套路保证。
- cost = 基础 + 每块平台×单价(比走跳贵,鼓励能走跳就别建造;比绕远路便宜,该建造果断建造)。
- **代价**:放置只能走预设套路(垂直垒/平搭桥)。创造性放置(应对怪地形)留待以后,接口留口子。

挖掘同理:优先考虑做成"挖穿一段"的宏动作,而非逐帧挖输入原语。

### 3. 去重:状态量化(仅为收敛,不为建模)
连续状态会爆炸,用细网格分桶去重:
- Px 量化到 ~4px,Py ~4px,Vx ~0.5,Vy ~0.5,Grounded
- 同桶视为重复,保留 g 最小的
- **关键**:量化只防重复搜索,地形永远由模拟说了算,不参与"能否站立"判断。
  这与现在"格子既去重又建模"的致命混淆彻底分开。

### 4. 启发式
A*/加权A*:h = 到目标的物理下界(水平距离/maxRun + 垂直距离的跳跃帧估算)。
状态空间大,启发式 + 量化剪枝是性能关键。可用加权 A*(次优但快)。

### 5. 目标判定
目标是"玩家中心落在目标格附近且 grounded(或可放平台落脚)"。
用真实 Px/Py 判,不取整。

### 6. 执行
规划输出**输入序列**(每帧按什么键)。执行 = 重放 + 实时闭环纠偏(NavState.Jump 已是此思路,可复用/泛化)。
因为规划用真实物理,重放天然贴合。

### 7. 未来机动扩展点(架构验证)
- 钩爪:加 `ControlInput.Grapple`,Step 实现钩爪物理,加输入原语"射向X"。搜索自动整合。
- 翅膀:加 `WingTimeLeft` 状态 + "持续上升"输入。
- 二段跳/加速靴:改 Params/Step 的跳跃/加速逻辑。
框架、搜索、执行**零改动**。这是格子方案做不到的。

## 性能挑战(主要风险)

- 状态空间比格子大几个数量级,模拟每帧有成本。
- 缓解:好启发式 + 激进量化去重 + 限制扩展分支(只枚举有意义的输入,如朝目标方向)+ 加权A* + node budget。
- 可能需要:分层(粗规划方向 + 细规划动作)、或 anytime 算法(先出次优路径,有时间再优化)。
- **这是唯一的真风险**。若性能不可接受,退化为"粗格子引导方向 + 段间状态空间求动作"的混合。

## 分阶段实施(每阶段可独立验证)

- **Phase 0 — 可行性原型**:写一个独立的状态空间搜索(新文件,不动现有 A*),只支持 走/跳,
  在简单地形上规划"从A到B"。验证:能出路径、性能可接受。**不接入,纯实验**。
- **Phase 1 — 半砖/斜坡验证**:在原型上测今天那些复现点(半砖、斜坡 pillar)。
  确认状态空间方案天然正确(应无需任何特殊处理)。
- **Phase 2 — 建造宏动作**:放平台/挖掘做成搜索可调用的宏动作边(垒柱/搭桥套路),
  **不**作逐帧输入原语(见 2b 踩坑结论)。撤掉逐帧放置补丁,node 退回纯物理状态。
- **Phase 3 — 执行接入**:输入序列 → 执行重放 + 闭环。先在受控目标(NavWand)测。
- **Phase 4 — 替换**:状态空间规划替换现有 A*,旧 A* 保留作回退/对照,稳定后删。
- **Phase 5 — 机动扩展验证**:加一个新机动(如二段跳)验证架构可扩展性。

## 与现有代码的关系

- **复用**:PhysicsSimulator(引擎)、Params(物理参数)、执行层闭环思路、HTTP/NavWand 接口。
- **替换**:PathPlanner 的格子 A*(Plan 主体 ~1000 行)、SubPx/FromPx、所有手写 action 边生成。
- **保留过渡**:旧 A* 不立即删,新旧并存对照,稳定后退役。

## 为什么 whatever cost 也值得

- 稳定:消灭翻译层,半砖/斜坡/亚像素/速度依赖类 bug 系统性消失。
- 可扩展:未来机动零框架改动,这是项目长期方向(WoF 及以后需要大量机动)的唯一可行架构。
- 地基已有:PhysicsSimulator 是最难的部分,已经写好且验证过。

## 待决策

1. 走这条路确认?
2. 从 Phase 0 原型开始(不动现有代码,纯实验验证可行性+性能)?
3. 性能红线:规划耗时多少可接受?(影响量化粒度/启发式激进程度)
